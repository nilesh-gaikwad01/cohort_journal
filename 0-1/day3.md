// Express Notes

``` what I learnerd Today? ```

- node js and its runtime
- backend communication Protocol
- Express basic 
- routes , status codes
 

 what is ECMAScript 
 what is Javascript
 what is Node.js
 What is Bun


- v8 engine  is  javacript compiler which is written in cpp , used to  convert js code into netive computer known code which is 0 and 1. 

- node js is javacript runtime env means in this js code compile in native language like brower

- 2012 to 13 V8 engine by adding some extra features (like APIs, server-side logic,  HTTP requests, routing, middleware, and database interactions) in the node js and from this nodejs used in backend for runnig the javacript in backend like browser.

- before the nodejs ,  java(springboot), Golang, Php is used in javascript for making the backend.


``` HTTTP protocol client side ```

protocols (http/htpps),Adresses(Url/IP/port), Route , HeaderBody and Query Parameter, Methods(POST,GET ,PUT , PATCH ,DELETE) 



- what is RPC 
- How the url work behind the DNS 
- How IP work behind the DNS
- 

```HTTP server side ```

Response header, Response body, Status code.
 
- how to send the body in post using the body pasrser library
- what is Load Balancer and how it work in Networking
- how to use Fetch API in backend
- fs module in node js

- what is enviroment variable in your project